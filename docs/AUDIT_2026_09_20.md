# SKILLER — Audit Matrix 2026-09-20

Аудит по техническому брифу (Google Doc, handoff) относительно FROZEN MVP SPEC v1.0 и PATCH 1.1.
Статусы: DONE / PARTIAL / MISSING / BROKEN / NOT_VERIFIED.

## 0. Исходник и baseline (Спринт 0)

| Факт | Значение |
|---|---|
| Repository | github.com/ivanvasiluk-maker/skiller-platform |
| GitHub origin/main | `14a111fad823b6233b90f3217ce6fc37db3d5ca1` (Round 6.1) — проверено свежим `git ls-remote`/`fetch` 2026-09-20 |
| Контрольные ревизии брифа | `052e2e3`, `e158cee`, `3d1b4e7` — **ОТСУТСТВУЮТ** в origin (все ветки, свежий fetch) и на диске (поиск `PATCH_1_1_AUDIT_PLAN.md`, `0009_conversational_relationship_layer.sql` по Downloads — 0 совпадений) |
| Локальная рабочая копия | `skiller-platform-main/` была на `1b3f59a` (предок 14a111f) + 1 нетривиальное изменение (`cloudflare-env.d.ts`: +OPENAI_API_KEY/OPENAI_MODEL/OPENAI_TRANSCRIBE_MODEL типы, сохранено). Fast-forward до `14a111f`, ветка `feat/conversation-relationship-layer` |
| Environment | Windows, Node v24.18.0 (≥22.13.0 ✓), npm 11.16.0 |
| Baseline gates | `lint` PASS · `typecheck` PASS · `test:core` PASS · `test:d1` PASS · `test:integration` PASS* · `build` PASS (логи: `work/audit-logs/`) |

\* `test:integration` падает, если рядом лежит `.env.local` с `OPENAI_API_KEY` (wrangler 4 подхватывает его → включается AI-путь → событие `mechanism_generated` в week-cycle ломает `noAiEvents: true`, scripts/test-d1-integration.mjs:470). Без `.env.local` — PASS. **Дефект изоляции теста**, не регрессия кода.

### БЛОКЕР (бриф §3)
Локально проверенная ревизия `3d1b4e7` «Refine conversation-first chat experience» (и предки `e158cee`, `052e2e3`) не существует ни на GitHub (ни одна ветка), ни локально. Артефакты PATCH 1.1 (`drizzle/0009_conversational_relationship_layer.sql`, `docs/PATCH_1_1_AUDIT_PLAN.md`, quick replies, typing indicator, inline intervention cards) отсутствуют в дереве `14a111f`. По брифу работа от старой ветки с «восстановлением по памяти» запрещена без решения владельца → требуется решение: предоставить архив/ветку ИЛИ санкционировать реализацию conversation-слоя заново по спецификации брифа поверх `14a111f`.

## 1. Audit matrix

| # | Требование (бриф/spec) | Статус | Доказательство | Фактическое поведение | Разрыв со спецификацией | Риск регрессии | Минимальное исправление | Критерий проверки |
|---|---|---|---|---|---|---|---|---|
| 1 | Conversation-first главный экран после onboarding | PARTIAL | `app/trainer-app.tsx` (screen="conversation"), `app/page.tsx` | Есть история сообщений, аватар/идентичность тренера, composer, online-статус; onboarding → разговор | Нет quick replies, typing animation, inline intervention cards, mic в trainer-app, маркера «Сегодня»; structured-поля (kind/signal/urge/intensity) торчат формой | Средний | Добавить shell-элементы спринта 2 | Ручной smoke desktop+mobile |
| 2 | Day 1: разговор → микро-действие → open loop | PARTIAL | `lib/trainer-data.ts` (action="situation"), `lib/skiller-data.ts` `recommendSkill()`, `lib/trainer-continuity.ts` `openLoop` | Ситуация → Skill Engine → trainer_plan; open loop вычисляется из plans | Open loop не персистентен с полями topic/planned_action/expected_time/follow_up_due/priority; нет событий `conversation_started`/`open_loop_created` | Высокий (расчётное состояние расходится с событиями) | Migration + persist open loop + 2 события | Тест: после refresh loop сохранён с полями брифа |
| 3 | Day 2: приоритет due loop, контекстный follow-up | PARTIAL | `lib/trainer-continuity.ts` `day2CheckIn`, `buildTrainerContinuity()` | На day 2 клиент получает prompt про вчерашний план | Нет событий `follow_up_shown`/`follow_up_answered`; нет гарантии приоритета follow-up перед новым flow на входе в разговор | Средний | Приоритизация due loop в orchestrator + события | Тест Day1→Day2 в integration worker |
| 4 | DONE → Success Analysis → success factor | MISSING | `lib/outcome-policy.ts`, `lib/pilot-event-schema.ts` | Есть `action_done` + outcome поля; success factor не хранится и не влияет на следующую реплику | Нет `outcome_done` (имя), `success_factor_identified`, success factor структуры | Средний | Ветка DONE в conversation pipeline + таблица success_factors | Success factor виден orchestrator и меняет реплику (тест) |
| 5 | PARTIAL → Behavioral Chain Analysis | MISSING | — | Результаты: done/failed/more; «partial» отсутствует | Нет `outcome_partial`, нет chain analysis по итогу частичного выполнения | Средний | Добавить result=partial + ветку анализа | Тест: partial → структурированная цепочка, без сброса к новому skill |
| 6 | NOT_DONE → Missing Link Analysis (без автозамены skill) | MISSING | — | `action_failed` → resize_after_failed (автоматический resize по policy) | Нет `outcome_not_done`, `missing_link_started/completed`; policy сразу ресайзит, PATCH 1.1 требует сначала анализ разрыва | Высокий (конфликт outcome-policy-v2 с PATCH 1.1) | Ветка missing link до resize; Action Resize только по результату анализа | Тест: not_done → missing_link_* события, skill не заменён автоматически |
| 7 | SKILL_REJECTED → rejection_reason в intervention memory | MISSING | — | Нет отказа от skill как исхода; low helpfulness → replace_low_fit | Нет `skill_rejected`, нет хранения rejection_reason, нет запрета повторного предложения без нового основания | Средний | Таблица intervention_memory + ветка отказа | Тест: рекомендация не защищается, повтор не предлагается без основания |
| 8 | Voice = тот же conversation pipeline | PARTIAL | `app/skiller-app.tsx` (record→transcribe), `lib/situation-analysis.ts` `transcribeAudio()` | Транскрипция через OpenAI, текст попадает в recommend flow skiller-app | В trainer-app (основной разговор) mic отсутствует; нет `voice_message_sent`; нет permission/retry UX в разговоре | Средний | Mic в trainer composer → тот же action="message"/"situation" | Parity-тест: одинаковый input → одинаковый state transition |
| 9 | Safety не зависит от LLM | DONE | `lib/trainers.ts` `requiresSafetyRoute()` + статичный `safetyMessage`; `lib/character-bible.ts`; integration endpoint /safety-cycle, /ai-fallback | Safety-route детерминирован, safety copy статичен, флаг safety_flag; тесты PASS | — | — | — | /safety-cycle + /ai-fallback зелёные |
| 10 | Deterministic fallback при ошибке/таймауте LLM | DONE | `lib/free-talk.ts` `produceFreeTalkReply()` (validateTrainerReply → fallback), `lib/skiller-data.ts` `selectSkill()` | Таймаут/malformed/hostile → fallback; onTimeoutIsFallback и т.д. = true в тесте | — | — | — | Integration /ai-fallback PASS |
| 11 | Skill Engine сохранён | DONE | `lib/outcome-policy.ts` (outcome-policy-v2), `lib/outcome-history.ts`, тесты `tests/outcome-policy.test.ts`, /adjustment-cycle | 5 ветвей (first_try/repeat/transfer/resize/replace) подтверждены тестами | — | — | — | outcome-policy fixtures зелёные |
| 12 | Обязательные события PATCH 1.1 (14 шт.) | MISSING | `lib/pilot-event-schema.ts` (35 событий, event-schema-v2) | Есть аналоги под другими именами: chat_started, action_done/failed, skill_recommended | Из 14 обязательных событий брифа (conversation_started, follow_up_shown/answered, open_loop_created/resolved, outcome_done/partial/not_done, skill_rejected, missing_link_*, chain_analysis_*, success_factor_identified, voice_message_sent) нет ни одного под требуемым именем | Высокий (аналитика PATCH 1.1 не собирается) | Расширить реестр (не удаляя старые) + payload spec + dedup + тест на каждое | /event-completeness + новые события в тесте |
| 13 | Idempotency / дедупликация | DONE | `lib/request-idempotency.ts`, `lib/pilot-events.ts` (INSERT OR IGNORE), UNIQUE attempt_id в outcomes | Retry/refresh не дублирует события и запросы; /outcome-idempotency PASS | — | — | — | Integration idempotency сценарии PASS |
| 14 | Migrations trail | PARTIAL | `drizzle/0000`–`0006` | Применённые миграции не менялись; trail целостен | `0009_conversational_relationship_layer.sql` отсутствует (0007/0008 тоже) | Высокий при рассинхроне с production | Новые изменения только новыми файлами 0007+ | Rehearsal на копии D1 |
| 15 | Onboarding / trainer selection / settings | DONE | `lib/trainer-data.ts` (onboard), `lib/trainer-settings.ts`, /onboarding-flow + /settings-continuity тесты | Onboarding + выбор тренера + смена настроек с событиями; тесты PASS | — | — | — | Integration PASS |
| 16 | Therapist workflow | PARTIAL | `db/schema.ts` `psychologist_access`, skiller-app «Психолог» tab | Consent toggles работают | Поверхности психолога (client list/brief/route editor) из IA не реализованы | Низкий для MVP (вне frozen spec core loop) | Отдельный этап, не блокер | — |
| 17 | Responsive / a11y / keyboard | NOT_VERIFIED | `app/trainer-app.tsx` (aria-label, role, htmlFor), tailwind max-w-md | Частичные aria-атрибуты есть | Полный focus order/contrast/mobile keyboard audit не выполнен; real-phone smoke не выполнен | Средний | Спринт 12 чек-лист | Keyboard-only проход + phone smoke PASS/FAIL |
| 18 | Secrets/config (.openai/hosting.json, project_id) | DONE | `.openai/hosting.json` project_id = `appgprj_6aa1a76f59e481918b65e459a25f2e19` — совпадает с брифом; `docs/ENVIRONMENT.md`; `tests/release-checklist.test.ts` (секрет не в бандле) | Конфигурация консистентна | — | — | — | deploy:check PASS |
| 19 | Deploy tooling + rollback | PARTIAL | `scripts/deploy.mjs` (check→build→migrations→deploy), `docs/DEPLOY.md` | Dry-run check есть; rollback описан (wrangler rollback + ручной D1 export) | Production deploy и сопоставление SHA этой сессией не выполнялись; production URL не проверен | Высокий | Спринты 13–14 | Production URL отвечает, SHA сопоставлен |
| 20 | Conversation Orchestrator (11 блоков контекста перед ответом) | MISSING | `lib/free-talk.ts`, `lib/trainer-data.ts` | Free talk получает историю + профиль тренера; нет единого структурированного контекста (USER PROFILE/OPEN LOOPS/GOALS/BEHAVIORAL+INTERVENTION MEMORY/SUCCESS FACTORS/SITUATION/SKILL ENGINE RESULT/SAFETY STATE) | Оркестратор как контракт отсутствует | Высокий (ключевой контракт PATCH 1.1) | Собрать buildConversationContext() + тест состава | Snapshot-тест контекста; fallback при ошибке модели |

## 2. Что переиспользуется без изменений
- Skill Engine: `lib/outcome-policy.ts`, `lib/outcome-history.ts`, `lib/skiller-data.ts` (recommendSkill/selectSkill).
- Safety: `requiresSafetyRoute`, `safetyMessage`, `safety_flag`, character-bible guardrails (`validateTrainerReply`).
- Аналитика: `lib/pilot-events.ts`, `pilot-event-schema.ts`, cohorts, export queue, sheets exporter (реестр только расширяется).
- Идемпотентность: `lib/request-idempotency.ts`, INSERT OR IGNORE паттерны.
- Continuity-вычисления: `lib/trainer-continuity.ts` (openLoop/day2CheckIn/days4to6/gapReturn) — как источник логики приоритета.
- Тестовая инфраструктура: core/d1/integration gates, deploy tooling.

## 3. Что расширяется
- `lib/pilot-event-schema.ts`: +14 событий PATCH 1.1 (старое не трогаем).
- `app/trainer-app.tsx`: quick replies, typing, inline action cards, mic, Enter/Shift+Enter (Enter уже частично есть — проверить), «Сегодня».
- `lib/free-talk.ts` → Conversation Orchestrator: структурированный контекст из 11 блоков.
- `lib/trainer-continuity.ts`: приоритет due loop при входе; события follow_up_*.

## 4. Что требует schema/API/UI изменений
- **Schema (новые миграции 0007+):** `open_loops` (topic, planned_action, created_at, expected_time, status, follow_up_due, priority), `success_factors`, `intervention_memory` (rejection_reason, missing links), при необходимости `conversation_events`.
- **API:** ветки результата done/partial/not_done/skill_rejected в `/api/trainer` (result enum сейчас done/failed/more — расширение, не переименование); оркестратор-контекст в message/situation pipeline.
- **UI:** inline intervention cards, quick replies, typing indicator, mic в trainer composer, follow-up первым сообщением дня.

## 5. Что опасно менять
- Применённые миграции 0000–0006 (только новые файлы).
- `lib/outcome-policy.ts` ветка failed→resize: PATCH 1.1 требует missing link ДО resize — менять через feature-ветку pipeline, не ломая outcome-policy-v2 fixtures.
- Реестр событий: запрещено удалять/переименовывать (action_done и т.п. остаются; новые события — дополнительные).
- `requiresSafetyRoute` и статичный safety copy.
- `safety_flow_used` и прочие события, уже попавшие в production Sheets.

## 6. Что блокирует production
1. **БЛОКЕР:** отсутствие контрольной ревизии `3d1b4e7` (нужно решение владельца).
2. Conversation layer PATCH 1.1 не реализован (пп. 1–8, 12, 20 матрицы).
3. Изоляция integration-теста от `.env.local` (OPENAI_API_KEY ломает `noAiEvents`).
4. Production deploy + сопоставление SHA + ручной smoke (desktop + реальный телефон) не выполнены.

## 7. Regression map (топ-риски)
- Расчётный open loop (из plans) может разойтись с персистентным после миграции — нужен backfill и двойное чтение на переходный период.
- Дубли событий при retry: новые события обязаны идти через `recordPilotEvent` с детерминированным id.
- LLM-реплика не должна предлагать skill вопреки outcome policy/safety — валидация `validateTrainerReply` + запрет self-selection уже есть, расширить на новые ветки.
- Voice/text расхождение: транскрипт обязан входить в тот же `trainerCommand` pipeline.
- `.env.local` в тестах — зафиксировать `SKILLER_AI_DISABLED=1` или чистку env в test-d1-integration.mjs.

---
Составлено: 2026-09-20, HEAD `14a111f` (ветка `feat/conversation-relationship-layer`). Все статусы подтверждены файлами/командами; логи — `work/audit-logs/`.
